import 'package:flutter/material.dart';

/// Application color palette inspired by Gabon flag and premium feel.
class AppColors {
  static const Color primary = Color(0xFF0B6E4F); // Deep Emerald Green
  static const Color secondary = Color(0xFFF4C430); // Warm Yellow
  static const Color accent = Color(0xFF0077B6); // Ocean Blue
  static const Color background = Color(0xFFF7F9FA); // Soft Light Grey
  static const Color text = Color(0xFF1E1E1E); // Dark Charcoal

  // Dark mode variants
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkSurface = Color(0xFF1E1E1E);
  static const Color darkText = Color(0xFFE0E0E0);
}
